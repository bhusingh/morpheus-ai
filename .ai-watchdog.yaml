# ai-watchdog configuration
# This file configures ai-watchdog for this project itself.

rules:
  pack: strict
  custom: ./rules/

instructions:
  - CLAUDE.md

on_violation:
  BLOCK: reject
  WARN: log
  INFO: count

output:
  format: text
  file: null

stats:
  enabled: true
  report_every: 50
